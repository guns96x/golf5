# D:\geminibase — розкладка

Це корінь робочої теки, а не проєкт. Контекст проєкту лежить нижче.

**Спочатку прочитай [CURRENT_STATE.md](CURRENT_STATE.md)** — оперативний зріз стану: що вже доведено, що в роботі, що заборонено шукати заново.
**Факти про машину та правила доказовості — у [golf5/CLAUDE.md](golf5/CLAUDE.md)**
(Golf 5 1.9 TDI BLS, Bosch EDC16U34, SW 1037391847). Не досліджуй репозиторій замість цих файлів.

## Три теки

| Тека | Що це | Git |
|---|---|---|
| `golf5\` | проєкт калібрування: A2L, BIN, логи, інструменти, документація | репозиторій `guns96x/golf5`, робоча гілка `claude/prompt-review-iuvqhg` |
| `base\` | бібліотека джерел: 78 PDF у `knowledge\library`, 21 чернетка у `knowledge\drafts`, реєстр `library_registry.json` | окремий репозиторій; **самі файли корпусу в git не кладемо** |
| `golf5\ecu-kb\` | локальна база знань (SQLite + FTS5) над корпусом і A2L | у складі `golf5`; сама БД і корпус у `.gitignore` |

`ecu-kb` живе всередині `golf5`, бо це інструмент проєкту й має версіонуватись
разом із ним. Корпус у `base\` — окремо, бо `git push` це поширення, а не
локальна копія.

## Звідки починати роботу з базою знань

```bat
cd D:\geminibase\golf5\ecu-kb
python kb.py status
python kb.py claims
python kb.py search "<запит>"
```

Інструкція — `golf5\ecu-kb\README.md`. Корпус грепати руками не треба.
